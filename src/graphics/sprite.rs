use image::RgbaImage;
use image;
use std::io::Cursor;


pub struct Sprite {
    image: RgbaImage
}

impl Sprite {
/*
    pub fn new(image_name: String) -> Sprite{
        image::load(Cursor::new(image_name)
            ,
            image::PNG)
            .unwrap().to_rgba()
    }
*/
}